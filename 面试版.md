## WHY mlp
- **LSTM/Transformer** 具有极强的**时序归纳偏置**（即它们天生假设输入是有序的、连续的）。
    
- **MLP** 的归纳偏置是**无序的、通用的**。
    
- 当你的特征工程把时间轴“压扁”成标量后，这些标量在截面上对所有股票是**齐次（Homogeneous）**的。此时，每一行数据都变成了一个独立的样本，股票之间满足**可交换性（Exchangeability）**，这完美契合了 MLP 的优化目标。

## 特征工程
### 为什么是"累计"
- 看累计收益 = 看"过去一学期的平均表现"
- 一个月的数据噪声太大（可能是偶然），但 3 个月、6 个月、12 个月的趋势更能反映"这只股票是不是真的强"。

### 5 个窗口 [1, 3, 6, 9, 12 个月]
**不同投资者的记忆长度不同**：
- 短线交易者看 1-3 个月（短期动量）
- 中线投资者看 6 个月
- 长线投资者看 12 个月
把 5 个窗口都给模型，让模型自己学"对于预测下个月的涨跌，哪个时间尺度的信息最有用"

### **关键函数设计**：

- `rolling_group_apply()`：按 PERMNO 分组做滚动计算（如累计收益用 $\prod(1+r)-1$）
- `cross_sectional_zscore()`：每个月对所有股票做横截面标准化，消除因子的截面不平稳性
- `assemble_feature_matrix()`：最终把 DataFrame 打包为 `(X, y, ids, dates)` 的 numpy 格式，供 PyTorch DataLoader 使用


## 计算超参流程
**第一步**：`features.py` → 把原始数据变成 10 个数字的向量

**第二步**：`models.py` → 决定网络长什么样：
```
输入 10 个数字 → [64个神经元] → [32个] → [16个] → 输出 1 个预测值
```

**第三步**：`train_grid_search.py` → 穷举尝试不同组合：
```
尝试 1: 层=[64,32,16], 激活=ReLU, dropout=0.1, 学习率=0.001
尝试 2: 层=[64,32,16], 激活=ReLU, dropout=0.2, 学习率=0.001
尝试 3: 层=[128,64],   激活=ELU,  dropout=0.1, 学习率=0.0005
... (全部组合)
```
然后选**验证集上误差最小**的那组参数，存到 `best_hyperparameters.txt`。

每只股票在每个月被表征为一个 10 维的特征向量（过去 1-12 个月的累计收益和换手率），这就是我说的'截面快照'。MLP 独立地对每个快照打分，最终按分数做排序选股。因为输入是平坦向量而非时间序列，
#### loss function 作用
在mlp 训练过程中，给其表现进行奖惩。mlp根据反馈进行反向传播。

#### 如何检验？
mlp对新数据直接打分不接受奖惩，进行交易，看最后交易结果。

---

## 数据预处理（`data_preprocess.py`）

**流水线式设计**（`prepare_panel_data` 一步到位）：
```
原始CSV → 日期解析 → 缺失值处理 → 生成基础变量(r, to) → 生成目标变量(r_{t+1})
```

| 步骤    | 函数                      | 设计考量                                  |
| ----- | ----------------------- | ------------------------------------- |
| 1. 读取 | `load_raw_csvs()`       | 支持通配符批量拼接多个时段的 CSV                    |
| 2. 日期 | `parse_dates()`         | 统一 datetime + 按 (PERMNO, date) 排序     |
| 3. 清洗 | `clean_core_columns()`  | 分组前向填充，再丢弃仍缺失的行                       |
| 4. 变量 | `add_basic_variables()` | `r = RET`, `to = VOL / (SHROUT×1000)` |
| 5. 目标 | `add_target_return()`   | 分组 shift(-1) 得到 $r_{i,t+1}$           |
每步都有 `validate_columns()` 做防御性校验。