# Chapter 1 Merge 1.1 and 1.2 - 2026-05-11

Source reviewed: `paper/已修改/chapter1_introduction.md`.

Question: can `1.1 Background and motivation` and `1.2 Research gap` be merged to make the Introduction shorter?

## Decision

Yes. Merge them.

The current `1.1` and `1.2` repeat the same logic from two angles:

- loss functions are underexplored relative to architecture and feature engineering;
- MSE/MAE optimise point-prediction accuracy rather than long-short portfolio ranking;
- robust and directional losses motivate the hybrid-loss design.

This is enough for one concise opening section. A separate `Research gap` section is not necessary because Chapter 2 already carries the detailed literature review and gap positioning.

## Recommended final Chapter 1 structure

```text
1.1 Background, motivation, and research gap
1.2 Research objectives
1.3 Research questions
1.4 Contributions and report structure
```

This is cleaner than keeping five Introduction subsections. It also avoids making the Introduction look like a mini Chapter 2.

## Replacement for merged 1.1

Use this to replace current `1.1` and `1.2`:

```markdown
## 1.1 Background, motivation, and research gap

Machine-learning models are now widely used for cross-sectional stock-return prediction and long-short portfolio construction. In this setting, the loss function is not just a technical training choice: it determines which prediction errors the model treats as important. Yet most return-prediction studies vary architectures and feature sets while keeping the objective at generic regression losses such as Mean Squared Error (MSE) or Mean Absolute Error (MAE).

This default is not fully aligned with the portfolio task. A long-short portfolio depends mainly on the cross-sectional ranking of predictions, not on their calibrated point values, and monthly equity returns are heavy-tailed. A model can therefore reduce MSE while still ranking the most economically relevant names poorly, or devote excessive training weight to noisy tail observations that do not improve portfolio construction.

Existing loss-function families address parts of this problem. Robust losses reduce the influence of heavy-tailed residuals, while directional losses such as MADL and GMADL [7, 8] reward correct return signs and magnitude-weighted directional alignment. What remains less clear is how regression, robust, directional, and hybrid losses compare under the same data, feature input, model, and portfolio rule. This report addresses that gap through a controlled empirical study of loss-function design for portfolio-oriented return prediction.
```

## Why this is better

The merged version keeps the necessary reader path:

```text
ML return prediction -> loss function matters -> MSE/MAE mismatch -> robust/directional partial solutions -> controlled hybrid-loss study
```

It removes:

- the separate mini literature-gap section;
- repeated statements about architectures and features receiving more attention than losses;
- repeated references to long-short ranking;
- detailed methodology language about phases and claim boundaries.

## Renumbering after merge

After replacing `1.1` and `1.2`, renumber:

```text
Current 1.3 Research objectives -> 1.2 Research objectives
Current 1.4 Research questions -> 1.3 Research questions
Current 1.5 Contributions and report structure -> 1.4 Contributions and report structure
```

Update internal references if any chapter points to Chapter 1 section numbers.

## Further compression suggestions

After merging `1.1` and `1.2`, Chapter 1 can be shortened further:

1. In Research objectives, keep the four objectives but make each one sentence.
2. In Research questions, keep the three RQs but remove explanatory sentences if Chapter 6 already answers them clearly.
3. In Contributions, keep four bullets, but remove exact variant names except `m2_robust_gamma07` if the Introduction feels too result-heavy.

## Suggested target length

With this merge, Chapter 1 should target about 1,000-1,150 words.

That is enough for the marking scheme: it frames the project, states objectives and questions, and gives report structure, while leaving detailed literature review to Chapter 2 and detailed experimental design/results to Chapters 3 and 5.
